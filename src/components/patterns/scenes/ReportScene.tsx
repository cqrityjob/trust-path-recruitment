import { ClipboardCheck } from "lucide-react";
import { TileScene } from "./JobHubScene";

export function ReportScene() {
  return <TileScene icon={ClipboardCheck} bars={["88%", "64%", "72%"]} />;
}
