# Owner Supabase bootstrap trigger

This file is intentionally schema-neutral.

Purpose: create a post-integration merge event on `main` after the official Supabase GitHub integration for owner-controlled project `vcgwvtmzftmulmoxmufv` was configured with working directory `.` and production branch `main`.

The merge of this file is authorised only to trigger the official Supabase integration so it can reconcile and apply the repository's existing canonical migration history to the empty owner-controlled project.

This file contains no SQL, changes no application runtime configuration, does not alter Auth, Storage, AI/transcription, or Lovable, and must not be treated as a migration.

Current live Lovable Cloud backend remains `zrahptwsnjcdyzfywbeh` until a separately approved cutover.

Permanently excluded project: `mlvzmiutmyyqeuvjglco`.
