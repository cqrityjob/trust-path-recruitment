# PR A — executed validation

Date: 2026-09-23. Baseline: `bb4e849b43960f9c82907e08965e51c57a9a3261`. All database fixtures are synthetic. No test ran against production.

| Check                                   | Executed result                                                                                                                                                                                                                                                                      |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Native PostgreSQL 16 baseline `db:test` | All 311 migrations replayed; full existing suite passed.                                                                                                                                                                                                                             |
| Native PostgreSQL 16 final `db:test`    | All 312 migrations replayed; full suite passed, including every existing RLS/rollback/connection-race suite.                                                                                                                                                                         |
| Security Work behavior                  | **322 assertions**, run before and after actual rollback/reapply (644 checks across those two runs).                                                                                                                                                                                 |
| Security Work planted defects           | **12/12 detected** by the original assertions. Wrong SQLSTATE/unrelated failures are rejected. Database savepoints restore each mutation.                                                                                                                                            |
| Security Work real-connection races     | **3/3 passed:** concurrent bootstrap, approval before citation deletion, citation deletion before approval.                                                                                                                                                                          |
| Rollback safety                         | Adopted work refused; a planted external dependency refused the entire transaction; empty stand-down left zero domain objects; reapply succeeded.                                                                                                                                    |
| Existing source negative controls       | **48 suites / 1,423 mutations** passed at `907c2a4`; all files restored byte-for-byte and tree clean. Subsequent SQL-only corrections have their own direct database controls; CI reruns the existing source controls on the final PR head.                                          |
| TypeScript                              | Application and `scripts:typecheck` passed.                                                                                                                                                                                                                                          |
| Application build                       | Passed using frozen lockfile dependencies. No runtime source change in this PR.                                                                                                                                                                                                      |
| Changed TypeScript lint / formatting    | Passed; generated types preserve every pre-existing byte/nullable override.                                                                                                                                                                                                          |
| Full lint baseline comparison           | Baseline **943 errors / 78 warnings**, final **942 / 78**; **zero new diagnostics**. Comparison excludes local `.claude` worktrees from the working checkout. The ordinary local `lint` command also traversed those unrelated worktrees and failed; full lint is not claimed green. |
| Release guards                          | Migration safety/duplicates, SQL security, release parity/frontier, deploy plan and its negative controls, schema-first, nullable RPC contract passed. Deploy plan contains only `20261210090000`; release gates intentionally remain closed pending owner application.              |
| Supabase local security advisors        | Baseline and final both **18 findings**, byte-identical results; **zero added**. Existing 15 no-policy INFOs, one definer-view ERROR, two public-extension WARNs. Local bootstrap differs from hosted extensions.                                                                    |
| Hosted security advisors                | Read-only baseline only; no Security Work objects exist there yet. See [hosted evidence](hosted-baseline.md).                                                                                                                                                                        |

Raw native replay evidence: [baseline log](evidence/baseline-db-test.log.gz), [final branch log](evidence/branch-db-test.log.gz). The logs include the full existing-suite summary; its historical aggregate does not enumerate every separately printed check, so no invented overall assertion total is reported.

The realistic `auth.jwt()` test helper now reads simulated verified claims. This exposed a previously bypassed Passport session check: its old fixture supplied authenticated claims without an Auth session. Five synthetic sessions and matching session IDs correct that fixture; its **43 existing assertions** pass with the actual guard exercised. No production authentication or session rule changed.

Independent review found a triage-attribution bug during development. The final migration prohibits rewriting a decided rationale without an explicit new state decision and preserves both decisions in immutable audit details. The final suite checks same-status edits, erasure, successive attribution, and restores the original defect in negative control 12 to prove detection. Private-function grants/search paths are inspected directly, including a planted PUBLIC-execution regression.

CI conclusions are recorded against the exact final head in the draft PR. This document records local execution, not unexecuted product-browser, deployment, provider or pilot evidence. The complete Security Work browser journey belongs to the application phases.
